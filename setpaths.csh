#!/bin/csh
# This is a csh utility script to set the paths to the current
# directory of OpenCache. It must be sourced in the local directory
# like this:
# source setpaths.csh

setenv OPENCACHE_HOME="`pwd`/generator"