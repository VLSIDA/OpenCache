#!/bin/bash
# This is a bash utility script to set the paths to the current
# directory of OpenCache. It must be sourced in the local directory
# like this:
# source setpaths.sh

export OPENCACHE_HOME="`pwd`/generator"